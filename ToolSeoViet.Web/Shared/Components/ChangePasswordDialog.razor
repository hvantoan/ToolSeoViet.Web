<MudDialog>
    <TitleContent>
        <MudText Typo="Typo.h6">Đổi mật khẩu</MudText>
        <MudSpacer />
        @*<MudIconButton Icon="@Icons.Filled.Close" OnClick="Cancel" />*@
    </TitleContent>
    @*<DialogContent>
        <MudForm @bind-IsValid="@success">
            <MudTextField Label="Mật khẩu cũ" InputType="@InputType.Password" @bind-Value="item.OldPassword"
                          Required="true" For="@(() => item.OldPassword)" Disabled="loading" />
            <MudTextField Label="Mật khẩu mới" InputType="@InputType.Password" @bind-Value="item.NewPassword"
                          Required="true" For="@(() => item.NewPassword)" Disabled="loading" />
            <MudTextField Label="Xác nhận mật khẩu" InputType="@InputType.Password" @bind-Value="item.RepeatPassowrd"
                          Required="true" For="@(() => item.RepeatPassowrd)" Disabled="loading" />
        </MudForm>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel" Variant="Variant.Outlined" Color="Color.Default" Style="width: 150px;">Bỏ</MudButton>
        <MudSpacer />
        <MudButton Variant="Variant.Filled" Color="Color.Primary" Style="width: 150px;" OnClick="Save" Disabled="!success || loading">
            @if (loading) {
                <MudProgressCircular Size="Size.Small" Indeterminate="true" />
            }
            <MudText Class="ms-2">Lưu</MudText>
        </MudButton>
    </DialogActions>*@
</MudDialog>