using Microsoft.AspNetCore.Components;
using Microsoft.AspNetCore.Components.Forms;
using Microsoft.AspNetCore.Components.Web;
using MudBlazor;
using System;
using System.Threading.Tasks;


namespace ToolSeoViet.Web.Shared.Components {

    public partial class ChangePasswordDialog {
        //[CascadingParameter] public MudDialogInstance MudDialog { get; set; }
        //[Inject] public ISnackbar Snackbar { get; set; }
        //[Inject] public UserService UserService { get; set; }

        //private bool loading = false;
        //private bool success = false;

        //private readonly ChangePasswordRequest item = new();

        //private void Cancel(MouseEventArgs args) {
        //    this.MudDialog.Cancel();
        //}

        //private async Task Save(MouseEventArgs args) {
        //    try {
        //        await this.UserService.ChangePassword(this.item);
        //        Snackbar.Add("Đổi mật khẩu thành công!", Severity.Success);
        //        this.MudDialog.Close();
        //    } catch (ManagedException ex) {
        //        this.Snackbar.Add(ex.Message, ex.ErrorLevel);
        //    } catch (Exception ex) {
        //        this.Snackbar.Add(ex.Message, Severity.Error);
        //    }
        //}
    }
}